<% if @instructors.empty? %>
  <%= 'Nenhum professor cadastrado! '%>
<% end %>

<% @instructors.each do |instructor| %>
  <%= link_to instructor.name, instructor %>
<% end %>

<%= link_to 'Voltar', root_path %>
<%= link_to 'Registrar um Professor', new_instructor_path %>
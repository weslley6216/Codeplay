<% if @instructors.empty? %>
  <%= t('message.no_instructor_registered') %>
<% end %>

<% @instructors.each do |instructor| %>
  <%= link_to instructor.name, instructor %>
<% end %>

<%= link_to t('links.back'), root_path %>
<%= link_to t('instructor.register'), new_instructor_path %>